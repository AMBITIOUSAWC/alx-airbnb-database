- `aggregations_and_window_functions.sql` - Uses SQL aggregation and window functions:
  - Counts total bookings per user.
  - Ranks properties based on total bookings using RANK().
- `subqueries.sql` - Uses both correlated and non-correlated subqueries:
  - Finds properties with average rating > 4.0.
  - Identifies users with more than 3 bookings.

